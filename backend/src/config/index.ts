export const appName = 'todoApp'
export const isProduction = false
export const port = 3000
export const mongoosePort = 27017
export const mongodb = { url: `mongodb://localhost/${appName}` }
export const apiURL = `http://localhost:${port}`